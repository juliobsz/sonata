<Window x:Class="Sonata.Desktop.Views.ConversationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Background="{DynamicResource WindowBackgroundBrush}"
        Title="Sonata" Height="800" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <ui:Button Grid.Row="0"
                    Content="New conversation"
                    Padding="1, 2"
                    Margin="0, 0, 0, 6"
                    Width="100"
                    HorizontalAlignment="Center"
                    Command="{Binding NewChatCommand}"/>
                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Conversations}"
                         SelectedItem="{Binding CurrentConversation}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4">
                                <TextBlock Text="{Binding Id}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <ListBox Grid.Row="0" 
                         ItemsSource="{Binding Messages}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8" Margin="4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="LightBlue" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Role}" Value="assistant">
                                                <Setter Property="Background" Value="LightGray" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" />
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Grid Grid.Row="1"
                      Width="500"
                      Margin="0, 10, 0, 0"
                      HorizontalAlignment="Center">
                    <ui:TextBox
                                MinHeight="48"
                                MaxHeight="160"
                                Padding="12, 10, 52, 10"
                                Text="{Binding CurrentInput, UpdateSourceTrigger=PropertyChanged}"
                                AcceptsReturn="True"
                                TextWrapping="Wrap"
                                VerticalContentAlignment="Top"
                                VerticalScrollBarVisibility="Auto"
                                PreviewKeyDown="CommandInputPreviewKeyDown" />
                    
                    <ui:Button x:Name="SendButton"
                               Width="36"
                               Height="36"
                               Margin="0, 0, 6, 6"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Bottom"
                               Appearance="Primary"
                               Icon="{ui:SymbolIcon ArrowUp32}"
                               Command="{Binding SendCommand}"
                               ToolTip="Send"
                               />
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
